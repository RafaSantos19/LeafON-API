services:
  mosquitto:
    image: eclipse-mosquitto:2.0
    container_name: leafon-mosquitto
    restart: unless-stopped
    ports:
      - "1883:1883"
    volumes:
      - ./docker/mosquitto/mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
